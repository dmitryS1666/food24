<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Кастомная тема, наследующая от AppCompat -->
    <style name="Theme.food24Events" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowActionBar">false</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        
        <!-- Required AppCompat color attributes -->
        <item name="colorPrimary">@color/custom_date_picker_header_color</item>
        <item name="colorPrimaryDark">@color/custom_date_picker_main_color</item>
        <item name="colorAccent">@color/custom_date_picker_header_color</item>
    </style>

    <style name="Theme.food24Events.Launch" parent="Theme.food24Events">
        <item name="android:windowBackground">@drawable/splash_screen_background</item>
    </style>

    <style name="Theme.food24Events.Splash" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="android:windowBackground">@drawable/splash_screen_background</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowActionBar">false</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
    </style>
</resources>